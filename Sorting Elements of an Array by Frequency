for i in range(int(input())):
    n=int(input())
    arr=list(map(int,input().split()))
    arr=sorted(arr)
    a=sorted(arr,key=arr.count,reverse=True)
    #a=a[::-1]
    for i in a:
        print(i,end=" ")
    print()
